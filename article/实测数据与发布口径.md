# 「后来呢？」实测数据与发布口径

## 一、网站研发时间

| 节点 | 时间 |
|---|---|
| `app.py` 本地创建 | 2026-08-03 22:06:17 |
| 第一版可运行提交 `7b1b73a` | 2026-08-04 01:02:11 |
| 两者墙钟时间差 | 约 2 小时 56 分钟 |

这个时间来自本地文件与 Git 提交时间线，只能描述“第一版网站从开始落盘到首次提交”的研发跨度。它不是 Doubao-Seed-Evolving 独立生成网站的时间，也不等于持续工作时长；不要写成 one-shot 生成结果。

可用口径：

> 第一版可运行网页从本地开始落盘到首次 Git 提交，墙钟时间约 2 小时 56 分钟。后续又继续增加 BYOK、脱敏、词云、年度热力图、移动端适配和测试。

## 二、Seed-Evolving API 实测

测试日期：2026-08-03。输入为仓库公开的 50 条虚构消息，覆盖 2025-01-03 至 2025-12-31。

| 指标 | 结果 |
|---|---:|
| API 请求次数 | 3 |
| 输入 Tokens | 11,182 |
| 输出 Tokens | 42,983 |
| 总 Tokens | 54,165 |
| 累计 API 延迟 | 713.032 秒，约 11 分 53 秒 |
| 重试次数 | 0 |
| 三次请求状态 | 全部 `completed` |

阶段拆分：

| 阶段 | 延迟 | 输入 Tokens | 输出 Tokens |
|---|---:|---:|---:|
| 制定分析计划 | 94.735 秒 | 1,447 | 4,958 |
| 全量时间线分析 | 329.093 秒 | 3,793 | 19,677 |
| 证据审计 | 289.204 秒 | 5,942 | 18,348 |

## 三、结果评测

| 指标 | 结果 |
|---|---:|
| 预埋跨月证据链 | 6 条 |
| 正确召回 | 6 / 6 |
| 报告引用的不同证据 ID | 50 个 |
| 无效证据 ID | 0 |
| 人工抽查事实结论 | 20 / 20 有原文支持 |
| 审计阶段修改 | 29 个 JSON 叶子字段 |

边界：这是人工设计的 50 条公开虚构样例，不能外推成任意真实聊天上的通用准确率，也不能表述为模型“没有幻觉”。

## 四、推荐正文段落

> 我用 50 条覆盖一整年的虚构消息跑了一次深度模式：3 次 API 调用共消耗 54,165 Tokens，累计耗时约 11 分 53 秒。样例里预埋的 6 条跨月线索召回 6/6，引用的证据编号没有出现无效 ID；最后的审计阶段又修改了 29 处报告细节。这个结果只对应公开样例，不外推到任意真实聊天。

## 五、证据文件

- `output/live_smoke.json`：API 返回、逐阶段 Tokens 与耗时；
- `output/evaluation.json`：召回、证据完整性与审计影响；
- `TESTING.md`：完整测试方法、长上下文边界和发布限制。
